# Chrome Keyboard Extension - 設計書

## 概要
Chromeブラウザ上でVimライクなキーボード操作を可能にする拡張機能のMVP版

## 目的
マウスを使わずにキーボードだけでWebページを効率的にナビゲートできるようにする

## 主要機能

### 1. スクロール操作
- `j`: 下にスクロール
- `k`: 上にスクロール
- `d`: 半ページ下にスクロール
- `u`: 半ページ上にスクロール
- `gg`: ページトップへ移動
- `G`: ページボトムへ移動

### 2. リンクヒントモード
- `f`: リンクヒントモードを起動
  - ページ内の全てのクリック可能な要素に番号を表示
  - 番号を入力することでその要素をクリック
  - `Esc`で終了

### 3. 検索・フォーカス
- `/`: 検索モード（ページ内検索）
  - 検索ワードを入力してEnter
  - 検索結果を黄色でハイライト表示
  - `↑`/`↓`キーで検索結果間を移動
  - 現在フォーカス中の結果はオレンジ色で表示
  - `Ctrl + Enter`で検索結果が含まれるリンクを新しいタブで開く
  - 画面右下に検索結果のカウンター表示（例: "1 / 15"）
  - `Esc`で検索モードを終了
- `i`: 最初の入力フィールドにフォーカス

### 4. ナビゲーション
- `H`: 戻る
- `L`: 進む
- `r`: リロード

## 技術スタック

### 言語
- JavaScript (バニラJS)
- HTML/CSS

### Chrome Extension API
- Manifest V3を使用
- Content Scripts でページに機能を注入

## ファイル構成

```
/
├── manifest.json         # 拡張機能の設定ファイル
├── content.js           # メインロジック（キーボードイベント処理）
├── content.css          # リンクヒントなどのスタイル
├── README.md            # 使い方
└── DESIGN.md            # 本ファイル
```

## 実装の詳細

### manifest.json
- Manifest V3形式
- content_scriptsでcontent.jsとcontent.cssを全ページに注入
- 必要な権限のみを要求

### content.js
**主要コンポーネント:**

1. **KeyboardHandler**
   - キーボードイベントをリッスン
   - 入力フィールドにフォーカスがある場合は無効化

2. **ScrollController**
   - スムーズスクロールを実装
   - 各種スクロール操作を提供

3. **LinkHintMode**
   - クリック可能な要素を検出
   - ヒント（番号）を生成・表示
   - ユーザー入力を処理してクリック実行

4. **SearchMode**
   - ページ内検索機能
   - TreeWalkerを使用してテキストノードを走査
   - 検索結果をspan要素でラップしてハイライト
   - 検索結果配列を保持して管理

5. **SearchNavigationMode**
   - 検索結果間のキーボードナビゲーション
   - ↑↓キーで前後の結果に移動
   - 現在フォーカス中の結果を視覚的に強調
   - Ctrl+Enterで検索結果が含まれるリンクを新しいタブで開く
   - 検索結果のカウンター表示

### content.css
- リンクヒントのスタイリング（黄色背景、オレンジ枠）
- 検索結果のハイライトスタイル
  - 通常の検索結果: 黄色背景
  - 現在フォーカス中: オレンジ色背景、白文字、太字
- 検索情報表示（右下のカウンター）
- 視認性の高いデザイン
- ページのスタイルと干渉しないようにz-indexを調整

## MVP制約事項

### 含まれる機能
- 基本的なスクロール操作（j/k/d/u/gg/G）
- リンクヒントモード（fキー）
- 高度な検索機能
  - ページ内テキスト検索とハイライト
  - 検索結果のキーボードナビゲーション（↑↓キー）
  - 検索結果からリンクを新しいタブで開く（Ctrl+Enter）
  - 検索結果カウンター表示
- ブラウザナビゲーション（H/L/r）
- 入力フィールドへの自動フォーカス（iキー）

### 含まれない機能（将来の拡張）
- 設定画面（カスタマイズ可能なキーバインド）
- タブ操作
- ブックマーク管理
- 高度なテキスト選択
- カスタムコマンド
- ビジュアルモード

## ユーザーエクスペリエンス

### フィードバック
- リンクヒントモード時は視覚的に明確なヒントを表示
- 検索モード時は検索結果をハイライト表示
- 検索ナビゲーション時は現在位置と総数を表示（例: "3 / 15"）
- 現在フォーカス中の検索結果は異なる色で強調表示

### パフォーマンス
- イベントリスナーは効率的に実装
- DOM操作は最小限に
- 大量のリンクがある場合も快適に動作
- TreeWalkerを使用した効率的なテキストノード走査
- 検索結果のナビゲーションはスムーズスクロールで快適

## セキュリティ考慮事項
- ユーザーの入力データは保存しない
- 外部通信は行わない
- ページのJavaScriptと干渉しないように実装

## インストール方法
1. Chrome拡張機能の開発者モードを有効化
2. 「パッケージ化されていない拡張機能を読み込む」で本ディレクトリを選択
3. 任意のWebページで動作開始

## 今後の改善案
- オプション画面の追加
- キーバインドのカスタマイズ
- ブラックリスト/ホワイトリスト機能
- より高度なVimライクな機能（ビジュアルモード、マーク等）
- 複数言語サポート
